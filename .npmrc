# NODE_ENV=production is set in this environment, which makes npm default to
# omit=dev and skip vite/tailwindcss/@vitejs/plugin-react. Force dev deps so
# `npm install` + `npm run build` work regardless of NODE_ENV. (include wins
# over omit in npm.)
include=dev
